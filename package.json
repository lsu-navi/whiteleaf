{
  "name": "tantanfilm",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build step'",
    "deploy": "wrangler pages deploy . --project-name=tantanfilme",
    "preview": "npx serve ."
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
